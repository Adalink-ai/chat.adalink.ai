export const MAX_FILE_SIZE = 20 * 1024 * 1024; // 20MB
export const MAX_FILES = 10;
export const ACCEPTED_FILE_TYPES = 'image/*,video/*,audio/*,.pdf,.doc,.docx,.txt,.csv,.json';

// Polling configuration
export const POLL_INTERVAL = 2000; // 2 seconds
export const MAX_POLL_ATTEMPTS = 60; // ~2 minutes total (60 * 2s = 120s)

